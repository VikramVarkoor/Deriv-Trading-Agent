// Global Cypress support file — runs before every e2e spec.
// Import any custom commands or third-party plugins here.

// Currently no custom commands needed — Cypress's built-in commands are enough.
// This file must exist for Cypress to find the support entry point.
export {};
